window.addEventListener('load', function(){
    alert("here we do");
     setInterval(linkedin, 3000);
});

function linkedin() {
       if(IN.User.isAuthorized()) {
            //TODO: XML request to server to render homepage
       }

 
}